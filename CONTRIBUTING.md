# Contributing

Fork the repository, create a focused feature branch, and open a pull request against the default branch. Run `npm ci` and `npm run validate` before submitting. Pull requests must pass automated checks, receive code-owner approval, and resolve all review threads. Maintainers squash-merge approved changes.

Never commit credentials, production data, or generated dependency folders.
